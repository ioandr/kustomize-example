apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - stub-service.yaml
  - stub-deployment.yaml

configMapGenerator:
  - name: app
    behavior: merge
    literals:
      - downstreamServiceUrl=http://stub:9080
