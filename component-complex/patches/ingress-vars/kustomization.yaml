apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
vars:
  - name: NAMESPACE
    objref:
      apiVersion: apps/v1
      kind: Deployment
      name: app
    fieldref:
      fieldpath: metadata.namespace

configurations:
  - kustomize-configuration.yaml